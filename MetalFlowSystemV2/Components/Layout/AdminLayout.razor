@inherits LayoutComponentBase
@using MudBlazor

@*
    This Layout is STATIC (no @rendermode).
    It allows the Router to pass the @Body (RenderFragment).
    The actual UI chrome and interactivity are handled by the page's <AdminShell> wrapper.
*@

<div id="admin-layout-root">
    @Body
</div>
